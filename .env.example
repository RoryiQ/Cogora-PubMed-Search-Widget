# OpenRouter API Key (for AI summaries & insights)
OPEN_ROUTER_DEV_API_KEY=your_openrouter_api_key_here

# Default Webhook URL (for saving articles to Airtable)
DEFAULT_WEBHOOK_URL=https://hooks.airtable.com/workflows/your_webhook_id

# Optional: PubMed API Key (increases rate limit from 3 to 10 req/sec)
# PUBMED_API_KEY=your_ncbi_api_key_here

# Widget Access Keys (comma-separated)
# Leave empty or remove for open access (dev mode)
# Generate secure keys with: openssl rand -hex 16
WIDGET_ACCESS_KEYS=your_key_1,your_key_2
